# Grand Project Summary

The Helix landing site is being assembled as a separate Vercel-deployable Next.js 14 (App Router) + TypeScript project, distinct from the reference orchestrator at https://helix-ai-orchestrator.vercel.app/, which is only a visual/brand source. The foundation is complete: USER-179000 bootstrapped the project shell (Tailwind, Inter via next/font, atoms under `components/ui/`, navbar/footer under `components/layout/`, `app/globals.css`, `vercel.json`, `.gitignore`); USER-986000 layered on the polished Helix brand tokens (`lib/brand.ts`, `@theme` in `app/globals.css`) and the section composition root `components/landing/Sections.tsx`; USER-969000 shipped the App Router scaffold with `app/layout.tsx`, `app/globals.css`, and `lib/brand.ts`; USER-303000 produced the full landing site at `app/page.tsx` using the brand theme. All marketing sections are implemented — top-of-page Hero, Features, and HowItWorks in `components/landing/hero/**`, `components/landing/features/**`, `components/landing/how-it-works/**` (also delivered earlier as `components/sections/Hero.tsx`, `Features.tsx`, `HowItWorks.tsx`), plus Personas, SocialProof, FAQ, CTA, and Footer at `components/Personas.tsx`, `components/SocialProof.tsx`, `components/FAQ.tsx`, `components/CTA.tsx`, `components/Footer.tsx` (with the accessible accordion primitive at `components/ui/accordion.tsx`), and copy-driven variants under `components/landing/personas/**`, `components/landing/social-proof/**`, `components/landing/faq/**`, `components/landing/cta/**`, `components/landing/footer/**`, backed by JSON in `content/hero.json`, `content/features.json`, `content/how-it-works.json`, `content/personas.json`, `content/social-proof.json`, `content/faq.json`, `content/cta.json`. Copy correctness around the Vercel reference URL was iterated through USER-616000 → USER-440000 → USER-384000 → USER-868000 → USER-633000 → USER-574000, which now reliably labels helix-ai-orchestrator.vercel.app as the "Reference site" while the landing site itself is the live destination. Page assembly (USER-217000) and the current deployment card USER-604000 are functionally complete — Hero, Features, HowItWorks, StatsBar, Personas, FAQ, and FinalCTA all render — but the Vercel build is currently blocked by an unresolved `@/app/components/sections/Hero` import path alias issue (previously chased via USER-348000 "Fix Hero import path resolution") that still fails to resolve between the `app/components/sections/` and `components/sections/` trees. In parallel, USER-176000 is finishing brand theme/typography/globals polish and USER-954000 still has the integrated Hero/HowItWorks/Features section card queued. Once the Hero import alias is reconciled so the project type-checks and builds cleanly, the site is ready to ship to Vercel.
