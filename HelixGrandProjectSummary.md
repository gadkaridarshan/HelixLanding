# Grand Project Summary

The Helix landing page is a Next.js 14 (App Router) + TypeScript site scaffolded at USER-179000 (`app/layout.tsx`, `app/globals.css`, `components/ui/`, `components/layout/`, `vercel.json`, `.gitignore`) and styled through the global brand theme token system delivered in `app/globals.css` (USER-765000, USER-176000) and the typed `lib/theme.ts` / `lib/brand.ts` palette. All marketing sections are done as independent components: Hero (`components/Hero.tsx` / `components/landing/hero/**`), HowItWorks (`components/HowItWorks.tsx`), Features (`components/Features.tsx`), Personas (`components/Personas.tsx` / `components/sections/Personas.tsx`), Social Proof (`components/SocialProof.tsx`), FAQ (`components/FAQ.tsx` / `components/ui/accordion.tsx` / `content/faq.json`), CTA (`components/CTA.tsx` / `components/FinalCTA.tsx`), and Footer (`components/Footer.tsx`), all composed into the route at `app/page.tsx` by USER-604000 / USER-217000. Reference-vs-live link copy has been corrected across `components/**` and `app/**` so https://helix-ai-orchestrator.vercel.app/ is consistently labelled as a reference site, not the live destination (USER-616000, USER-384000, USER-440000, USER-868000, USER-633000, USER-574000). The latest milestone card_done is USER-525000, which finalized Vercel deploy polish — adding `vercel.json`, `public/favicon.ico`, `public/og.png`, `src/app/layout.tsx.metadata.ts`, `src/app/opengraph-image.tsx`, and `src/app/icon.tsx` for SEO/social previews — without touching section components owned by the two in-progress cards (USER-310000 and USER-23000, both producing README.md). Those two remaining in_progress cards own the final README consolidation and full landing-site handoff; once they close, the site is ready for Vercel deployment.
