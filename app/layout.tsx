# Netlify-style redirects file is unused on Vercel, but keeping this
# placeholder commented out so copy-paste from other hosts never
# accidentally introduces conflicting redirect rules here.
# (See vercel.json for the canonical config used by Vercel.)