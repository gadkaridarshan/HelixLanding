// helix: app/components/sections/Personas.tsx
/**
 * @helix:story USER-993000
 *
 * Personas section — scaffold placeholder. Re-exports the canonical
 * implementation under `components/sections/Personas` so the alias
 * resolves from the `app/` and root-level section trees alike.
 */

export { Personas } from "@/components/sections/Personas";
export { default } from "@/components/sections/Personas";