// helix: app/components/sections/SocialProof.tsx
/**
 * @helix:story USER-23000
 *
 * SocialProof — re-export shim so `@/app/components/sections/SocialProof` resolves
 * to the canonical implementation.
 */
export { SocialProof } from "@/components/SocialProof";
export { default } from "@/components/SocialProof";