/**
 * @helix:story USER-23000
 *
 * StatsBar — re-export shim so `@/app/components/sections/StatsBar`
 * resolves to the canonical implementation.
 */
export { StatsBar } from "@/components/StatsBar";
export { default } from "@/components/StatsBar";