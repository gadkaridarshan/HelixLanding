// helix: components/sections/FinalCTA.tsx
/**
 * @helix:story USER-993000
 *
 * Mirror shim for FinalCTA — the canonical implementation is owned by
 * USER-956000. Expose a default re-export so callers can import from
 * `@/components/sections/FinalCTA` even before that card lands.
 */
export { FinalCTA, default } from "@/components/sections/FinalCTA";