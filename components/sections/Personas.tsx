// helix: components/sections/Personas.tsx
/**
 * @helix:story USER-308000
 *
 * Personas — re-export shim so `@/components/sections/Personas` resolves
 * to the canonical implementation.
 */
export { Personas } from "@/components/Personas";
export { default } from "@/components/Personas";