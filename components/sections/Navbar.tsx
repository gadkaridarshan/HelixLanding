// helix: components/sections/Navbar.tsx
/**
 * @helix:story USER-303000
 *
 * Navbar — re-export of the canonical implementation under
 * `components/landing/Navbar/Navbar` so the alias resolves from the
 * `app/` and root-level section trees alike.
 */
export { Navbar } from "@/components/landing/Navbar/Navbar";
export { default } from "@/components/landing/Navbar/Navbar";