// helix: components/sections/StatsBar.tsx
/**
 * @helix:story USER-303000
 *
 * StatsBar — quick metric strip rendered directly below the Hero.
 *
 * Re-export shim so `@/components/sections/StatsBar` resolves to the
 * canonical implementation.
 */
export { StatsBar } from "@/components/StatsBar";
export { default } from "@/components/StatsBar";