// helix: components/sections/SocialProof.tsx
/**
 * @helix:story USER-933000
 *
 * SocialProof — re-export shim so `@/components/sections/SocialProof` resolves
 * to the canonical implementation.
 */
export { SocialProof } from "@/components/SocialProof";
export { default } from "@/components/SocialProof";